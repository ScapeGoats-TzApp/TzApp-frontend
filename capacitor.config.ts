import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scapegoats.tzapp',
  appName: 'TzApp',
  webDir: 'dist/fusion-angular-tailwind-starter/browser'
};

export default config;
